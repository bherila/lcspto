@model LCSPTO.Mvc.Form

<form method="post" class="@Model.CssClass.ToString()">

    @{ Html.DroppableZone("FormFields").Title("Form Fields").Render(); }

    <button type="submit" class="btn btn-primary">Submit</button>
	<button type="button" class="btn" onclick="window.print();">Print</button>

</form>