@model lcspto_mvc.Models.SyncModel
@{
    Layout = null;
}
<SyncInfo>
@foreach (var x in Model.Subdirectories) {
    <dir name="@x" />
}
@foreach (var x in Model.FileItems) {
    <file len="@x.filelen" md5="@x.filemd5" name="@x.filename" />
}
</SyncInfo>